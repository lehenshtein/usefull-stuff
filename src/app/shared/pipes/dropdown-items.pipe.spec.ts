import { DropdownItemsPipe } from './dropdown-items.pipe';

describe('DropdownItemsPipe', () => {
  it('create an instance', () => {
    const pipe = new DropdownItemsPipe();
    expect(pipe).toBeTruthy();
  });
});
